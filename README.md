# Skill Agent Demo - AI 学习助手

基于 LLM 的智能学习助手，支持概念讲解、习题生成、闪卡制作等多种学习场景。

## 🚀 快速开始

### 启动服务

```bash
# 一键启动前后端服务
bash start_services.sh

# 或手动启动
cd backend && uvicorn app.main:app --host 0.0.0.0 --port 8088 --reload
cd frontend && npm start
```

### 测试接口

```bash
# 基础功能测试
cd backend/scripts
python test_part1_basic_features.py

# 上下文卸载效果演示（带可视化）
python test_context_offloading_demo.py
```

## 🎯 核心功能

### 1. 多技能支持
- **讲解 (Explain)**: 深度概念解释，包含直觉、定义、例子
- **练习 (Quiz)**: 智能题目生成，支持选择、填空、简答、判断
- **闪卡 (Flashcard)**: Anki 风格记忆卡片
- **思维导图 (Mindmap)**: 结构化知识图谱
- **笔记 (Notes)**: 自动整理学习笔记
- **学习包 (Learning Bundle)**: 组合多种技能的完整学习路径

### 2. 智能上下文管理（Context Engineering）

基于三大支柱实现长对话场景下的 token 优化：

#### 🔹 Offloading (上下文卸载)
- 大型 artifacts (> 500 tokens) 自动保存到文件系统/S3
- Context 中只保留轻量级引用和摘要
- **效果**: 单个 artifact 从 ~2000 → ~200 tokens (**90% 压缩**)

#### 🔹 Reduction (上下文缩减)
- 监控 context 使用率，超过 70% 自动触发缩减
- 修剪 (Prune) 冗长的 tool calls 和历史输出
- 递归摘要 (Condense) 早期对话轮次
- **效果**: Context 增长率从 83% → 24%

#### 🔹 Retrieval (按需检索)
- Agent 可主动调用工具检索历史内容
  - `read_artifact(artifact_id)` - 读取完整内容
  - `search_artifacts(query)` - 语义搜索
  - `list_artifacts(session_id)` - 列出索引
- **效果**: 按需加载 = 按需付费

**Token 消耗对比**:

| Turn | Before (完整加载) | After (智能卸载) | 节省 |
|------|-------------------|------------------|------|
| 1    | 5,255             | 2,450            | -53% |
| 2    | 9,625             | 3,200            | -67% |
| 3    | 13,500            | 3,500            | -74% |
| 10   | ~35,000           | ~5,000           | **-86%** |

**关键突破**: Context 不再线性增长，支持 10+ 轮长对话！

### 3. 智能意图识别（Intent Router）

**Phase 4 架构**: 纯粹的 Skill Registry 匹配，0-token 分类

- ✅ 基于关键词的快速匹配
- ✅ 支持混合意图检测（如："讲解光合作用，给我3道题"）
- ✅ 无 LLM fallback，避免 token 浪费

### 4. 会话管理

- **智能分段**: 基于内容长度和语义断点自动创建新会话
- **上下文继承**: 新会话继承前一个会话的压缩摘要
- **S3 持久化**: 会话日志和 metadata 自动上传到云端

## 📁 项目结构

```
skill_agent_demo/
├── backend/
│   ├── app/
│   │   ├── api/              # FastAPI 路由
│   │   ├── core/             # 核心模块
│   │   │   ├── context_artifact_manager.py    # 上下文卸载
│   │   │   ├── context_memory_manager.py      # 上下文缩减
│   │   │   ├── skill_orchestrator.py          # 技能编排
│   │   │   ├── intent_router.py               # 意图路由
│   │   │   └── memory_manager.py              # 记忆管理
│   │   ├── models/           # 数据模型
│   │   ├── prompts/          # LLM Prompt 模板
│   │   ├── services/         # LLM 客户端 (Kimi)
│   │   └── tools/            # Agent 工具集
│   ├── skills/               # 技能定义
│   └── scripts/              # 测试脚本
├── frontend/                 # React 前端
├── logs/                     # 日志目录
└── README.md
```

## 🔧 配置

创建 `.env` 文件：

```bash
# LLM 配置
NOVITA_API_KEY=your_novita_api_key
KIMI_MODEL=moonshotai/kimi-k2-thinking

# S3 配置
USE_S3_STORAGE=true
AWS_ACCESS_KEY_ID=your_access_key
AWS_SECRET_ACCESS_KEY=your_secret_key
AWS_S3_BUCKET=skill-agent-demo
AWS_REGION=us-east-1
```

## 🧪 测试场景

### Part 1: 基础功能
```bash
python backend/scripts/test_part1_basic_features.py
```

测试内容：
- ✅ 意图识别准确性
- ✅ 混合意图处理
- ✅ 技能执行质量
- ✅ Quiz 和 Flashcard 生成

### 上下文卸载演示（推荐！）
```bash
python backend/scripts/test_context_offloading_demo.py
```

可视化展示：
- 🎨 彩色进度条显示 Context Window 使用率
- 📊 实时 token 对比和节省计算
- 💰 成本节省估算
- ✅ 三大支柱验证报告

输出示例：
```
┌──────────────────────────────────────┐
│ Turn 2: 加载 compressed summary      │
│ Token: 7,625 (Context: +2,370)       │
│ 节省: ~1,630 tokens (40%)            │
│ Context Window: ████████░░░░ 7.6%    │
└──────────────────────────────────────┘
```

## 📊 性能指标

- **Prompt 优化**: 平均减少 78% prompt tokens
  - explain: 22K → 3.5K chars (-84%)
  - flashcard: 16K → 3.6K chars (-77%)
  - quiz: 23K → 5.1K chars (-78%)

- **上下文卸载**: 节省 70-86% tokens（长对话场景）

- **响应速度**: < 5s (简单查询), < 15s (复杂生成)

## 🔗 相关文档

- [FEATURES.md](./FEATURES.md) - 详细功能说明和架构文档
- [BUG_FIXES.md](./BUG_FIXES.md) - 已知问题和修复记录

## 📝 开发日志

**最新更新 (2025-11-24)**:
- ✅ 实现 Context Engineering 三大支柱
- ✅ 优化所有 Skill Prompts (-78% tokens)
- ✅ 修复 Intent Router LLM fallback 问题
- ✅ 修复 Quiz 显示 N/A 问题
- ✅ 增强测试脚本（可视化输出）

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

## 📄 许可

MIT License
